# 製作・現場管理アプリ 開発依頼仕様書

## 0. あなたへの依頼

大栄製作所（金属製作・加工業）向けの社内業務管理アプリを開発してください。

**重要：一度に全部作らないでください。** 下記「開発フェーズ」に従い、1フェーズずつ実装し、都度動作確認を求めてください。フェーズ1が承認されるまでフェーズ2に進まないこと。

### 利用者

**当面は開発依頼者1名が運用し、数ヶ月後に従業員（数名）へ展開します。**

- **権限分けは不要。** 全ユーザーが全機能・全データを閲覧・編集できてよい
- **ただしログインは実装すること。** 権限管理のためではなく、「誰が工程を完了させたか」「誰がチェックしたか」を記録するため。名前を毎回手入力させる運用は続かない
- 社内利用のため、認証は簡易なもので構わない（メール＋パスワード、または共有アカウント＋利用者選択など。実装が軽い方式を提案すること）
- マルチテナント対応・複雑な入力バリデーションは不要

### 動作環境

**最初からクラウドにデプロイする構成としてください。** ローカルPC限定の構成にはしないこと。

理由：運用者は自宅からアクセスし、従業員は工場内からアクセスします。ローカルPCに置くと自宅からアクセスできません。

- **無料枠で開始できるサービスを選定してください。** この規模（ユーザー数名、1日の登録件数は数件〜十数件）であれば無料枠に収まります
- 後から有料プランへ移行できること。プラン変更だけで済み、コード変更や再構築が発生しない構成にすること
- 無料枠特有の制約（一定時間アクセスがないとスリープする等）は許容します。ただし制約の内容は提案時に明示してください
- スマートフォン（iPhone中心）のブラウザから利用します。**ネイティブアプリは作らないこと。** レスポンシブ対応のWebアプリとし、ホーム画面に追加して使う想定です
- 画面はPC・スマホで共通で構いません。ただしスマホ表示時、**工程の完了ボタンと完成チェックには2タップ以内で到達できること**

### 図面ファイルの保管

図面（PDF/DWG/DXF）は容量が大きく、無料枠を最初に超える要因になります。以下のいずれかを提案してください。

- 既存のクラウドストレージ（Google Drive / OneDrive等）に保管し、DBにはリンクのみ持つ
- クラウドストレージサービスの無料枠を利用する

いずれの場合も、**DBにはパスまたはリンクのみを記録し、バイナリを格納しないこと。**

### 最優先事項

**データ構造の正確さを最優先してください。** 画面・認証・デザインは後から作り直せますが、データ構造は後から直せません。

### バックアップ

**データベースの自動バックアップを最初から組み込んでください。** 頻度は1日1回で構いません。復元手順もあわせて提示すること。

---

## 1. 業務背景

### 会社の業務フロー
```
客先から図面受領（PDF・DWG等）
    ↓
自社でバラシ図（製作用図面）を作成
    ↓
材料手配
    ↓
製作（切断〜溶接〜仕上げ）
    ↓
表面処理（自社 or 外注）
    ↓
完成チェック
    ↓
現場搬入
```

このアプリが対象とするのは **「製作〜完成チェック〜通知」** の範囲です。

### 将来構想（今回は作らない）
将来的に以下のアプリと連携する予定があります。今回のDB設計は、これらと連携できる余地を残してください。

- 図面解析・材料拾い出しアプリ
- 見積もりアプリ
- 材料発注アプリ
- 請求管理アプリ

具体的には、**将来「見積時の数量」と「実際に使った数量」を突き合わせられるよう**、部材明細を保持する構造にしておくこと（3-8参照）。

これらのアプリは**同一のデータベースを共有し、画面のみを分ける構成**とします。アプリごとにDBを分けて後で連携する方式は採りません。

---

## 2. 最重要の設計方針

### 2-1. フォルダではなくデータベースで持つ

現場の中に製品がある、という構造を扱いますが、**OSのフォルダ階層で実装しないでください。**

理由：フォルダ方式だと「今週納期の製品を全現場から抽出」「表面処理待ちの製品だけ表示」ができません。

**「見た目はフォルダ、中身はリレーショナルDB」** で実装してください。画面上は現場を開くと製品が並ぶフォルダのように見せ、データは正規化されたテーブルで保持します。

### 2-2. 進捗率は保存しない

進捗率（％）をカラムとして保存しないでください。**工程テーブルの完了フラグと重みから毎回算出**します。保存すると必ず実態とズレます。

### 2-3. 発注番号と内部IDを分ける

客先から来る「発注番号」は表示・検索用。テーブル間の紐づけには**内部の自動採番ID**を使ってください。

理由：発注番号は客先が採番するもので、変更・重複・枝番の可能性があります。内部IDを分けておけば、発注番号が変わっても紐づけが壊れません。

### 2-4. 記録は追記のみ

チェック記録・通知履歴は**追記のみ**とし、既存レコードを上書き・削除しないでください。再チェックした場合は新しい行を追加します。経緯を追えることが目的です。

### 2-5. 工程テンプレートは「コピー」する

製品登録時、工程テンプレートの内容を**工程テーブルにコピー**してください。参照にしないこと。

理由：参照にすると、テンプレートを編集した瞬間に過去の製品の工程まで変わり、履歴が壊れます。コピーなら製品ごとに自由に編集できます。

---

## 3. データベース設計

### 3-1. 現場テーブル（sites）

| カラム | 型 | 補足 |
|---|---|---|
| site_id | INTEGER PK | 自動採番。内部紐づけ用 |
| order_number | TEXT | 発注番号（客先採番）。表示・検索用 |
| site_name | TEXT NOT NULL | 現場名 |
| client_name | TEXT | 客先名 |
| site_address | TEXT | 現場住所 |
| delivery_due_date | DATE | 現場搬入納期 |
| manager_user_id | INTEGER FK → users | **この現場の担当者。完成チェック通知の送り先** |
| status | TEXT | 進行中 / 完了 / 保留 |
| note | TEXT | 備考 |
| is_deleted | BOOLEAN | 削除済みフラグ（物理削除しない） |
| created_at | DATETIME | |
| updated_at | DATETIME | |

### 3-2. 製品テーブル（products）

| カラム | 型 | 補足 |
|---|---|---|
| product_id | INTEGER PK | 自動採番 |
| site_id | INTEGER FK → sites | **現場への紐づけ** |
| order_number | TEXT | 製品単位で発注番号が異なる場合に使用。通常は空 |
| product_name | TEXT NOT NULL | 製品名 |
| quantity | INTEGER | 数量 |
| material | TEXT | 素材（SS400 / SUS304 等） |
| thickness | TEXT | 板厚（t9 等） |
| finish | TEXT | 仕上げ（溶融亜鉛めっき / 焼付塗装 / 生地 等） |
| drawing_number | TEXT | 自社バラシ図の図番 |
| process_due_date | DATE | 加工納期予定日（社内期限） |
| delivery_date | DATE | 現場搬入予定日 |
| material_status | TEXT | 未発注 / 発注済 / 入荷済 |
| material_arrival_date | DATE | 材料入荷予定日 |
| surface_type | TEXT | 自社 / 外注 |
| vendor_name | TEXT | 外注先名 |
| vendor_send_date | DATE | 持込予定日 |
| vendor_return_planned | DATE | 戻り予定日 |
| vendor_return_actual | DATE | **実際の戻り日** |
| status | TEXT | 未着手 / 製作中 / 完了 / 出荷済 |
| is_deleted | BOOLEAN | 削除済みフラグ（物理削除しない） |
| note | TEXT | 備考 |

**設計意図：**
- 数量が複数でも個体管理はしません（10台まとめて1レコード）
- 素材・仕上げは1製品1種類が前提
- **外注の予定日と実績日を両方持つこと。** この差分が蓄積すると「あの外注先は平均何日遅れる」が数値で見えるようになります
- 材料状態は進捗率に含めず、別カラムで管理。一覧上で色や記号で表示すること

### 3-3. 工程テンプレートテーブル（process_templates）

| カラム | 型 | 補足 |
|---|---|---|
| template_id | INTEGER PK | |
| template_name | TEXT | 「標準」「架台」等 |
| process_name | TEXT | 工程名 |
| weight | INTEGER | 重み（％） |
| sort_order | INTEGER | 並び順 |
| alert_message | TEXT | 着手時の注意喚起文（後述） |

**初期データ（テンプレート名：標準）**

| 並び | 工程名 | 重み |
|---|---|---|
| 1 | 切断 | 15 |
| 2 | カエリ取り | 8 |
| 3 | 穴あけ | 15 |
| 4 | 組立 | 18 |
| 5 | 溶接 | 22 |
| 6 | 仕上げ | 12 |
| 7 | 塗装・研磨 | 10 |

合計100。**この数値は画面上で編集可能にしてください。**

### 3-4. 工程テーブル（processes）

| カラム | 型 | 補足 |
|---|---|---|
| process_id | INTEGER PK | |
| product_id | INTEGER FK → products | **製品への紐づけ** |
| process_name | TEXT | |
| weight | INTEGER | 重み（％）。製品ごとに編集可 |
| sort_order | INTEGER | |
| is_completed | BOOLEAN | 完了フラグ |
| started_at | DATETIME | 着手日時 |
| started_by | TEXT | 着手した人 |
| completed_at | DATETIME | 完了日時 |
| completed_by | TEXT | **完了操作をした人** |

**started_by / completed_by は運用開始当初は1名分しか入りませんが、必ず最初から作成してください。** 後から追加すると過去データが全て空欄になり、遡って埋められません。

**進捗率の算出：** `完了した工程のweight合計 ÷ 全工程のweight合計 × 100`

### 3-5. チェック項目マスタ（check_items）

| カラム | 型 | 補足 |
|---|---|---|
| item_id | INTEGER PK | |
| item_name | TEXT | |
| sort_order | INTEGER | |
| is_active | BOOLEAN | 使用中フラグ |

**初期データ**

| 並び | 項目 |
|---|---|
| 1 | 材料・板厚は図面通りか |
| 2 | 寸法違いはないか |
| 3 | 穴の開け忘れ・位置違いはないか |
| 4 | 仕上げ忘れ・仕上げ違いはないか |
| 5 | 傷・打痕はないか |
| 6 | 溶接不良はないか |
| 7 | 数量は揃っているか |

**この項目は画面から追加・編集できるようにしてください。** 運用しながら不具合が出るたびに項目を足していく想定です。

### 3-6. チェック記録テーブル（check_records）

| カラム | 型 | 補足 |
|---|---|---|
| record_id | INTEGER PK | |
| product_id | INTEGER FK → products | |
| item_name | TEXT | チェック時点の項目名を保存（マスタ変更の影響を受けないため） |
| result | TEXT | OK / NG / 未確認 |
| checker_name | TEXT | チェック者 |
| checked_at | DATETIME | |
| note | TEXT | |

**追記のみ。上書き・削除しないこと。**

### 3-7. 部材マスタ（material_masters）

社内で使う材料の名称を統一するためのマスタ。将来の連携時、名称表記のゆれを防ぐ目的で用意します。

| カラム | 型 | 補足 |
|---|---|---|
| material_id | INTEGER PK | |
| material_code | TEXT | 社内コード |
| material_type | TEXT | 鋼板 / アングル / チャンネル / パイプ / フラットバー 等 |
| material_grade | TEXT | SS400 / SUS304 等 |
| size_spec | TEXT | t9 / L-50×50×6 / □50×50×2.3 等 |
| unit | TEXT | 枚 / 本 / m / kg |
| is_active | BOOLEAN | |

初期データは空で構いません。運用しながら追加していきます。

### 3-8. 部材明細テーブル（material_details）★将来連携の要

**このテーブルは当面ほぼ使いませんが、フェーズ1で必ず作成してください。** 後から追加すると既存データの作り直しが発生します。

| カラム | 型 | 補足 |
|---|---|---|
| detail_id | INTEGER PK | |
| product_id | INTEGER FK → products | **製品への紐づけ** |
| material_id | INTEGER FK → material_masters | マスタ参照（NULL可・自由入力も許容） |
| part_name | TEXT | 部材名（例：ベースプレート、柱、ブレース） |
| material_grade | TEXT | 材質 |
| size_spec | TEXT | サイズ・板厚 |
| length | REAL | 長さ（mm） |
| unit | TEXT | 単位 |
| quantity_estimated | REAL | **見積数量** |
| quantity_actual | REAL | **実績数量** |
| source | TEXT | 手入力 / 図面解析 / 見積アプリ |
| note | TEXT | |

**設計意図（重要）**

- 現状は製品テーブルに材質・板厚を1つずつ持っていますが、実際の製品は複数種類の部材で構成されます。この明細テーブルがその中身にあたります
- **quantity_estimated と quantity_actual を必ず分けて持つこと。** この2つの差分の蓄積が、将来の図面解析・見積アプリの精度そのものになります。1列にまとめると、後から振り返る手段が永久に失われます
- source カラムにより、手入力されたものか自動解析されたものかを区別します。将来、解析精度の検証に使います
- 材料手配は現場単位でまとめて行うことが多いですが、**データは製品に紐づけたまま保持し、発注時に現場で合算表示する**方針とします。製品→現場の集計は可能ですが、逆は不可能なためです

**将来の接続点**

| 将来のアプリ | このテーブルとの関係 |
|---|---|
| 図面解析・拾い出し | quantity_estimated に書き込む |
| 見積もり | 明細を読み、単価を掛けて見積を作る |
| 材料発注 | 現場単位で合算して発注書を作る |
| 請求管理 | quantity_actual を根拠に請求額を確定する |

### 3-9. ファイルテーブル（files）

| カラム | 型 | 補足 |
|---|---|---|
| file_id | INTEGER PK | |
| site_id | INTEGER FK → sites | 現場に紐づく場合 |
| product_id | INTEGER FK → products | 製品に紐づく場合（NULL可） |
| file_name | TEXT | |
| file_type | TEXT | PDF / DWG / DXF / 画像 等 |
| file_path | TEXT | 実ファイルの保存先パス |
| category | TEXT | 客先図面 / バラシ図 / 写真 / その他 |
| original_name | TEXT | アップロード時の元ファイル名 |
| extracted_text | TEXT | 図面から抽出したテキスト（判定用・検索用） |
| match_status | TEXT | 自動紐づけ / 手動紐づけ / 未分類 |
| match_confidence | REAL | 判定の確信度（0〜1） |
| uploaded_at | DATETIME | |

**ファイル実体は通常のディレクトリに保存し、DBにはパスのみ記録してください。** DBにバイナリを入れないこと。

保存先の構造は `ファイル保存ルート/現場ID/製品ID/ファイル名` とし、**アプリが起動しなくてもエクスプローラから図面が開ける状態**を維持してください。これはデータ消失に対する保険です。

### 3-10. ユーザーテーブル（users）

| カラム | 型 | 補足 |
|---|---|---|
| user_id | INTEGER PK | |
| user_name | TEXT | 表示名。工程完了・チェック記録に使用 |
| login_id | TEXT | |
| password_hash | TEXT | 平文保存しないこと |
| lineworks_id | TEXT | LINE WORKS の個人ID。通知の宛先に使用 |
| is_active | BOOLEAN | 退職者を無効化するため |

**権限カラムは不要です**（全員同一権限）。将来必要になった場合に追加します。

### 3-11. 設定テーブル（settings）

通知の送り先や判定日数など、運用しながら変わる値を保持します。**コードに直書きしないこと。**

| カラム | 型 | 補足 |
|---|---|---|
| key | TEXT PK | 設定キー |
| value | TEXT | 値 |

保持する設定の例：

| キー | 初期値 | 内容 |
|---|---|---|
| notify_check_group | — | 完成チェック通知の送り先グループ |
| notify_check_fallback_user | — | 現場に担当者が未設定の場合の送り先 |
| notify_daily_group | — | 朝のまとめの送り先グループ |
| notify_daily_time | 08:30 | 朝のまとめ送信時刻 |
| notify_days_before | 3 | 納期が近いと判定する日数 |

### 3-12. 通知履歴テーブル（notifications）

| カラム | 型 | 補足 |
|---|---|---|
| notification_id | INTEGER PK | |
| product_id | INTEGER FK → products | 朝のまとめの場合はNULL |
| notify_type | TEXT | 完成チェック / 朝のまとめ |
| target | TEXT | 送信先（グループ名・個人ID） |
| message | TEXT | 送信内容 |
| sent_at | DATETIME | |
| status | TEXT | 成功 / 失敗 |

---

## 4. 画面構成

アプリを開くと以下のメニューが表示されます。各項目から下位画面へ遷移し、戻るボタンで階層を上がります。

```
メニュー
├ 01 新規登録 ……… 図面から登録 ／ 手で登録 ／ 未分類 ／ 振り分け済み
├ 02 全現場・製品 … 現場一覧 → 現場ページ → 製品詳細
│                                └ 資料ページ
├ 03 納期順 ……… 製品を納期順に一覧 → 製品詳細 → 完成チェック
└ 04 作業終了リスト … 送信済みの製品
```

**02と03は同一データの並べ替えです。** 実装は共通の一覧コンポーネント＋並べ替え切り替えとし、画面を二重に作らないでください。一覧上部に「現場順／納期順」の切り替えを置き、どちらから入っても行き来できるようにします。

**新規登録の入口は01に一本化します。** 02（全現場・製品）の一覧画面には「＋ 現場を登録」は置きません。新しい現場は必ず01の「現場と製品をまとめて登録」フォームから作成し、既存の現場に製品を追加する場合のみ、現場ページの「＋ 製品を登録」を使います（詳細は4-4）。

### 4-1. 現場ページ

- 現場の基本情報（現場名・客先・発注番号・搬入納期・**担当者**）
- 現場の登録・編集画面で担当者をユーザー一覧から選択できること。この担当者が完成チェック通知の宛先になります
- **資料ページへの導線**
- その現場に属する製品の一覧（各製品に工程メーターを表示）

### 4-2. 資料ページ（現場ごとに1枚）

**現場に関するファイルを種類を問わず全て置く場所です。**

- 上部にアップロード領域を配置。**「ファイルを選ぶ」「写真を撮る」のボタンを必ず設置すること**（ドラッグ&ドロップだけではスマホから使えません）
- PCではドラッグ&ドロップ、クリップボードからの貼り付け（Ctrl+V）にも対応
- **アップロード時に種類や紐づけ先を選ばせないこと。** 放り込んだら即座に一覧へ並ぶこと
- 拡張子から自動で「図面（PDF/DWG/DXF）」「写真・画像」「その他」に仕分けて表示
- 図面は名前順、写真は日付順のサムネイル表示
- ファイルごとに**一行メモ**を後から付けられること（検索時に最も有効なため）
- 並べ替え（新しい順／名前順）と、**図面から抽出したテキストによる検索**

### 4-3. 製品詳細

- 製品情報（数量・素材・板厚・仕上げ・図番・材料状態・納期）
- 工程メーター（重みに応じた幅の帯で表示。完了・進行中・未着手を色分け）
- 工程ごとの着手／完了ボタン、実施者と日時の表示
- 全工程完了時のみ「完成チェックへ」が有効化される

### 4-4. 新規登録（メニュー01）

**図面の取り込みと新規登録を1つの画面にまとめます。** 実務上、図面が届いてから現場・製品を登録するため、作業が一続きになるためです。

画面構成は上から順に：

**1. 図面から登録**
- 「ファイルを選ぶ」「写真を撮る」ボタン＋ドロップ領域
- 取り込んだ図面を読み取り、**抽出した内容を入力欄に埋めた状態で登録画面を開く**（現場名・製品名・材質・板厚・図番など）
- 人は不足分を補い、誤りを直して登録する。図面に書いてあることを打ち直させないこと

**2. 図面がない場合の手入力**
- 「現場と製品をまとめて登録」フォーム1つに統一する（現場単独の登録画面は設けない）
- 電話等で受注が決まり、製品の詳細が未定の場合は、製品名を仮の値（例：「製品名未定」）で登録し、確定後に製品詳細から編集する
- 既存の現場に製品だけを追加したい場合は、現場ページの「＋ 製品を登録」から行う（これは01ではなく現場ページ側の導線）

**3. 未分類**
- 判定できなかったファイル。候補を提示して選ばせる

**4. 振り分け済み**
- 自動で紐づいたファイルの一覧。各ファイルに「変更」操作を用意

#### 登録フォームの構成

製品登録は項目数が多いため、以下のまとまりに分割して表示してください。1枚の長いフォームにしないこと。

| 区分 | 項目 |
|---|---|
| 製品 | 製品名 / 数量 / 図番 / 素材 / 板厚 / 仕上げ |
| 納期 | 加工納期予定日（自社の製作が終わる日）/ 現場搬入予定日 |
| 材料 | 材料の状態（未発注・発注済・入荷済）/ 入荷予定日 |
| 表面処理 | 区分（自社・外注）/ 外注先 / 持込予定日 / 戻り予定日 |
| 工程 | テンプレート選択 |

- **素材・仕上げはプルダウンとすること。** 自由入力だと表記がゆれ、後の集計・連携ができなくなります。「その他」の選択肢は残す
- 表面処理で「自社」を選んだ場合、外注関連の入力欄を非表示にする
- **実際の戻り日は登録時に入力させないこと。** 戻ってきた時点で製品詳細または編集画面から記録します
- 納期の2項目には意味の補足を添えること（どちらに何を入れるか混同しやすいため）

#### 編集

**独立した編集メニューは作らないでください。** 各画面から直接入ります。

- 現場ページ → 「編集」→ 現場の情報を修正
- 製品詳細 → 「編集」→ 製品の情報を修正
- 資料ページ → 各ファイルのメモ追加・紐づけ変更

編集画面は登録画面と同一のフォームを使い、値が入った状態で開くこと。

#### 削除（二重確認）

削除は可能とします。ただし**必ず二段階の確認**を挟んでください。

1. 編集画面の最下部に「削除」を配置（目立たない位置・警告色）
2. **一段目**：一緒に消えるものを具体的な件数で表示する（例：「製品3件・工程の記録・チェック記録・添付図面6件も消えます」）。「本当によろしいですか」だけでは読まずに押されます
3. **二段目**：確認のため「削除」と入力させる

**内部的には物理削除せず、削除済みフラグを立てて一覧から除外する方式を推奨します。** 画面上の見え方は削除と同じですが、誤操作時に復旧できます。図面が多数紐づいた現場を誤削除すると二重確認でも取り返しがつかないためです。

### 4-5. 設定画面

**通知の送り先・時刻・判定日数は、必ずアプリの画面から変更できるようにしてください。** コードや環境変数に直書きしないこと。担当者の異動、グループの作り直し、朝礼時刻の変更は運用中に必ず起こります。そのたびに開発を依頼するのでは実用に耐えません。

メニューまたはヘッダーから設定画面へ入れるようにし、以下を編集可能にします。

**LINE WORKS 通知先**

| 設定 | 内容 |
|---|---|
| 完成チェック通知：グループ | 送信先グループを指定 |
| 完成チェック通知：担当者未設定時の送り先 | 現場に担当者がいない場合の代替宛先 |
| 朝のまとめ：グループ | 送信先グループを指定 |
| 送信テスト | 保存した設定で**テスト送信できるボタンを用意すること** |

**テスト送信は必須です。** IDやグループの指定を誤っても、実際に送るまで気づけません。設定直後にその場で確認できるようにしてください。

**朝のまとめ**

| 設定 | 初期値 |
|---|---|
| 送信時刻 | 08:30 |
| 「納期が近い」と判定する日数 | 3日前 |
| 各区分の有効／無効 | 全て有効（加工納期・現場搬入・外注戻り遅れ・材料未入荷） |

**マスタ編集**

| 対象 | 内容 |
|---|---|
| 工程テンプレート | テンプレートの追加、工程名・重み・並び順の編集 |
| チェック項目 | 項目の追加・編集・並べ替え・無効化 |
| 着手時の注意喚起 | 工程ごとの注意文の編集 |
| 素材・仕上げの選択肢 | プルダウンに表示する候補の編集 |
| 外注先 | よく使う外注先の登録（入力候補として使用） |
| ユーザー | 従業員の追加・無効化、**LINE WORKS 個人IDの登録・変更** |

**通知は後から設定できること（重要）**

**LINE WORKS のボット登録を、アプリ稼働の前提条件にしないでください。** ボットの作成には管理者権限での作業が必要で、後回しになる可能性があります。それによってアプリ全体が使えないのでは本末転倒です。

- 通知設定が空でも、**現場・製品の登録、工程管理、完成チェック、図面の取り込みなど全機能が正常に動作すること**
- 完成チェックの送信ボタンは「通知先が未設定です」と表示し、**チェック記録の保存は通常どおり行うこと**。チェックが記録できなくなる作りにしないこと
- 朝のまとめは送信先が未設定なら実行をスキップし、エラーとして扱わないこと
- 設定画面でトークンとグループを登録した時点から、通知が有効になること

**担当者とLINE WORKS IDの紐づけも後から可能にすること**

ユーザーの LINE WORKS 個人ID（users.lineworks_id）は、**空のまま運用できる必要があります。**

- ユーザーは名前だけで登録でき、IDは後から追加できること
- 現場の担当者は、その人のIDが未登録でも設定できること
- IDが登録された人から順に通知が届くようになること。全員分揃うまで通知機能全体が止まる作りにしないこと
- 担当者のIDが未登録の製品については、グループへの送信のみ行い、**設定画面で指定した代替宛先にも送ること**
- ユーザー管理画面で、**ID未登録のユーザーが一目で分かる表示**を行うこと（例：「田中（ID未登録）」）。誰の設定が残っているかを探さずに済むようにするため

**設定画面にボット登録の手順を短く記載してください。** 「LINE WORKS管理画面でボットを作成 → トークンを貼る → 対象グループにボットを追加 → テスト送信」程度の流れで構いません。設定は一度きりの作業であり、担当者が変わった際に辿れる必要があります。

**LINE WORKSの接続情報（ボットのトークン等）の扱い**

接続情報は通常の設定と分け、**環境変数など安全な場所に保持してください。** ただし有効期限切れや再発行が起こりうるため、画面から更新できる手段も用意し、その際は現在値を伏字で表示すること。

### 4-6. 完成チェック

- チェック項目マスタから項目を読み込んで表示
- 各項目に該当する指示値を併記（例：「材料・板厚は図面通りか」の下に「指示：SS400 t3.2 ／ 図番 A-125」）
- **全項目のチェックを必須にしないこと**
- 「チェック完了・LINE WORKSへ送信」で送信し、製品の状態を完了に変更

### 4-7. 作業終了リスト

- チェック・送信が済んだ製品の一覧
- **データを別テーブルへ移動しないこと。** products.status を変更し、通常の一覧から除外されるだけとする
- 検索して過去案件を参照できること。将来の請求・実績集計で使用します

---

## 5. 機能仕様

### 5-1. 登録

- 現場の新規登録・編集
- 製品の新規登録時、**工程テンプレートを選択すると工程が自動生成される**（コピー方式）
- 生成後、その製品の工程は個別に追加・削除・重み変更が可能
- 工程の重み合計が100でない場合、**警告は出すが保存は許可する**（強制しない）

### 5-2. 横断ビュー（重要）

現場をまたいだ抽出ができること。これがフォルダ方式に対する最大の利点です。最低限以下を実装してください。

- 納期順の全製品一覧（今週納期 / 今月納期での絞り込み）
- 材料状態が「未発注」の製品一覧
- 外注に出したまま戻ってきていない製品一覧（vendor_send_dateがあり、vendor_return_actualが空）
- 未完了の製品一覧

### 5-3. 進捗表示
- 製品ごとに進捗をメーター（プログレスバー）で表示
- 工程ごとに**着手ボタン・完了ボタン**を配置
- **数値入力はさせないこと。** ボタンを押すだけで進捗が動く形にする

### 5-4. 着手時の注意喚起（重要）

工程の着手ボタンを押したとき、その工程に設定された注意文をダイアログで表示する。**確認して閉じるだけ。記録は不要。**

**ただし、注意文と一緒に該当製品の実データを必ず表示すること。** これが効果の本体です。

| 工程 | 表示する内容 |
|---|---|
| 切断 | 「材料・板厚は図面通りですか」＋ 素材・板厚・図面番号を表示 |
| 穴あけ | 「図面を確認しましたか」＋ 図面番号を表示 |
| 塗装・研磨 | 「仕上げ指示を確認しましたか」＋ 仕上げ内容を表示 |

文言だけでは読み飛ばされますが、具体的な数値（例：SS400 / t9 / 図面No.A-123）が出ていれば手元の材料と照合する動作が自然に発生します。

### 5-5. 完成チェック
- 全工程完了後、チェック画面を開ける
- チェック項目マスタから項目を読み込んで表示
- **全項目のチェックを必須にしないこと。** 注意喚起としての位置づけ
- チェック者名・日時を記録
- チェック完了操作をした時点で、チェック状態がそのまま記録される

### 5-6. ファイル取り込みと自動振り分け（フェーズ5）

#### アップロード方法
以下の3つを実装してください。

1. **画面からのアップロード** — 「ファイルを選ぶ」「写真を撮る」ボタン。PCではドラッグ&ドロップ・貼り付けにも対応。現場ページ・製品ページから行った場合は紐づけ先が確定しているため判定不要
2. **共有からの取り込み（重要・優先度高）** — スマートフォンのLINEやGmailで受け取った添付ファイルを、OSの共有メニューから本アプリへ送って取り込めるようにする
3. **監視フォルダ** — 指定フォルダにファイルが置かれたら自動で取り込む（紐づけ先の判定が必要）

**共有からの取り込みについて**

図面はLINEやメールで届くことが多いため、この経路が実務上もっとも使われます。

- Web App Manifest の `share_target` を利用し、ホーム画面に追加したWebアプリを共有先として登録できるようにしてください
- 共有で受け取ったファイルは「図面の取り込み」画面へ入り、通常の判定ロジックにかけます
- 共有元（LINE / メール / その他）が取得できる場合は files テーブルに記録すること

**Gmailの自動監視（受信したら自動で添付を取り込む）は今回の対象外とします。** 認証・接続設定の負担が大きいためです。共有経由で運用し、件数が増えた段階で改めて検討します。個人LINEについては外部から内容を取得する手段がないため、共有経由のみとします。

#### 中身の読み取り

**前提：社内にファイル命名規則はありません。** 現場名または製品名が入っていることが多いものの、名称の重複がありえます。そのためファイル名だけに依存せず、**ファイルの中身も読んで判定してください。**

| 形式 | 読み取り方法 | 備考 |
|---|---|---|
| DXF | テキスト形式のため直接パース。図面枠内の文字列（図番・現場名・材質・板厚）を抽出 | 最も確実 |
| DWG | バイナリ。DXFへ変換してからパース（ODA File Converter 等の利用を検討） | 変換手段は提案すること |
| PDF（CAD出力） | 埋め込みテキストを抽出 | 高速・高精度 |
| PDF（スキャン） | テキストが取得できない場合のみOCR/AIによる読み取りにフォールバック | 精度は落ちる前提 |

**処理順：まずテキスト抽出を試み、取得できなかった場合のみOCRに回すこと。** 常にOCRを使うのは無駄です。

抽出したテキストは files.extracted_text に保存し、**全文検索にも利用できるようにしてください。**

#### 紐づけ判定ロジック

優先順位は以下の通り。

1. 抽出テキスト内の図番が products.drawing_number と一致 → 製品に自動紐づけ
2. 抽出テキスト内の現場名・製品名がDB内の名称と一致
3. ファイル名から現場名・製品名を推定
4. いずれも該当なし・複数該当 → **未分類**

**同名候補が複数ある場合の絞り込み：**
- status が「完了」の現場は候補から除外、または優先度を下げる
- 直近に登録された現場を上位に表示する

#### 未分類の扱い（重要）

**確信度が低い場合は無理に自動振り分けしないでください。** 誤った製品に紐づくと発見が困難になります。

- 「図面の取り込み」画面内に未分類の一覧を表示する
- 各ファイルについて、**推定候補を確信度つきで複数提示し、クリック1回で選べる**ようにする
- 候補以外の選択肢として「現場を検索」「**新しい現場として登録**」を用意する
- ゼロから探させないこと

**現場を自動作成しないこと。** 「○○工場増築」「○○工場 増築工事」のような表記ゆれにより、同一現場が重複登録されるためです。新規登録は必ず人の操作を経ること。

#### 振り分け済みの表示（重要）

**自動で紐づいたファイルも一覧に表示し、「変更」操作を用意してください。**

自動判定は必ず誤りを含みます。結果が画面に出ていなければ誤りに気づけず、間違った現場に紐づいたまま放置されます。振り分け済み一覧は誤りを発見・修正するための画面です。

#### 判定履歴の蓄積

**手動で紐づけた場合、その対応関係を記録してください。**

| カラム | 補足 |
|---|---|
| pattern | ファイル名または抽出テキストの特徴 |
| site_id / product_id | 紐づけ先 |
| created_at | |

同様のパターンが次回現れたとき、この履歴を参照して自動判定に利用します。**運用するほど精度が上がる仕組みにすること。**

#### 自動リネーム

アプリ経由で取り込んだファイルは、保存時に `発注番号_製品名_図番.拡張子` の形式へリネームしてください。元のファイル名は original_name に保持します。これにより、今後蓄積するファイルについては命名の不統一が解消されます。

### 5-7. LINE WORKS通知

送信は**ボット**を作成して行います。管理者権限でボットを登録し、対象グループに追加する初期設定が必要です。

#### 通知の種類と送り先

| 通知 | タイミング | 送り先 |
|---|---|---|
| 完成チェック完了 | チェック完了操作の直後 | **Aグループ ＋ その現場の担当者** |
| 朝のまとめ | 毎朝の指定時刻 | **Aグループのみ** |

#### 担当者への振り分け（重要）

**現場ごとに担当者が異なるため、完成チェックの通知先は固定にしないでください。**

- 送信先は `Aグループ` ＋ `製品が属する現場の manager_user_id に紐づく LINE WORKS ID`
- 担当者は**現場の登録・編集画面から選択**します。設定画面で都度変更する運用にはしないこと
- 現場に担当者が未設定の場合は、設定画面で指定した代替の送り先へ送ること。**誰にも届かない状態を作らないこと**

朝のまとめはグループのみへ送り、個人への振り分けは行いません。

#### 完成チェック完了の通知
- 製品名・現場名・チェック結果（何項目中何項目）・チェック者を含める
- 送信内容のテンプレートは設定画面で編集可能にする
- 送信履歴を notifications テーブルに記録する

#### 朝のまとめ通知（重要）

**個別に都度飛ばさず、毎朝1回にまとめて送信してください。** 通知が多いと読まれなくなるためです。

含める内容：

| 区分 | 条件 |
|---|---|
| 加工納期が近い | 加工納期予定日まで規定日数以内、かつ未完了 |
| 現場搬入が近い | 現場搬入予定日まで規定日数以内 |
| 外注 戻り遅れ | 戻り予定日を過ぎているが、実際の戻り日が未入力 |
| 材料 未入荷 | 材料状態が「入荷済」でなく、入荷予定日が近い／過ぎている |

送信例：

```
【8/14 の予定】

■ 加工納期が近い
・架台A（○○工場増築）8/15
・手すり（○○工場増築）8/16

■ 外注 戻り遅れ
・ブラケットB-2（△△倉庫）
　○○メッキ工業／予定 8/10 ／3日超過

■ 材料 未入荷
・階段（△△倉庫）入荷予定 8/14
```

**該当がない日は送信しないこと。** 「本日は該当ありません」が毎朝届くと読まれなくなります。

設定できる項目：
- 送信時刻（初期値 8:30。変更可）
- 「納期が近い」と判定する日数（初期値 3日前）
- 各区分の有効／無効

#### 定時実行について（技術選定時の注意）

朝のまとめ通知は**時刻をトリガーに自動実行**されます。操作に応じて動く他の機能とは仕組みが異なり、**無料枠のホスティングでは定時実行が使えない、またはスリープ中に起動しない場合があります。**

技術選定の際、この点を必ず確認し、定時実行が可能な構成を提案してください。外部のスケジューラサービスを併用する形でも構いません。

---

## 6. 開発フェーズ

**各フェーズ完了時に動作確認を求め、承認を得てから次へ進んでください。**

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| 1 | DB設計・スキーマ作成・初期データ投入（**部材明細テーブルを含む**）、クラウドへのデプロイ、簡易ログイン | ブラウザからURLでアクセスでき、ログインできる |
| 2 | メニュー画面、現場・製品の登録と一覧（現場順／納期順の切り替え）、横断ビュー | 手入力でデータが登録・閲覧できる |
| 3 | 工程テンプレート、工程の着手/完了、進捗メーター | 進捗が動く |
| 4 | 着手時注意喚起、完成チェック、作業終了リスト、部材明細の手入力画面、**設定画面（マスタ編集）** | チェックが記録され、各マスタを画面から編集できる |
| 5 | 資料ページ、図面の取り込み、LINE WORKS通知 | 図面が正しく紐づき、通知が飛ぶ |

**フェーズ5は分量が多いため、以下の順で細分化して進めてください。**

| 5-a | 資料ページ＋画面からのアップロード（ファイル選択・カメラ） |
| 5-b | DXF/PDFのテキスト抽出と全文検索 |
| 5-c | 図面の取り込み画面（判定・未分類・振り分け済み一覧） |
| 5-d | 共有からの取り込み（share_target）、監視フォルダ |
| 5-e | DWG変換対応、スキャンPDFのOCR |
| 5-f | LINE WORKS通知（完成チェック）＋通知先設定・テスト送信。**それまでは送信処理をログ出力のスタブとし、通知なしで全機能が動く状態を維持すること** |
| 5-g | 朝のまとめ通知（定時実行） |

## 7. 実装時の注意

- コードにチェック項目や工程名を直書きしないこと。すべてマスタテーブルから読むこと
- 日付は必ずDATE型/DATETIME型で保存すること（文字列にしない）
- 画面は情報密度を優先。1画面で状況が把握できることを重視する
- 過度な装飾は不要。実用性を優先する

---

## 8. 最初にやること

このドキュメントを読んだら、いきなり実装を始めず、以下を提示してください。

1. 技術選定の提案（言語・フレームワーク・DB・**ホスティング先**）とその理由
2. **無料枠の具体的な制限内容**（容量・スリープ有無・帯域等）と、有料へ移行する場合の月額目安
3. 図面ファイルの保管方式の提案
4. フェーズ1のテーブル定義（DDL）
5. 仕様の中で不明瞭な点、または設計上の懸念点

**技術選定にあたっては、「無料で始められること」と「後から有料へ切り替えるだけで拡張できること」を条件としてください。** 移行時にコードの書き直しや別サービスへの引っ越しが必要になる構成は避けること。

承認後にフェーズ1の実装に入ってください。
